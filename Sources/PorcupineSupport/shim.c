// Intentionally empty. PorcupineSupport exists only to expose Picovoice's Porcupine C API
// (vendored headers + prebuilt libpv_porcupine.dylib under lib/) to Swift as the CPorcupine
// module - see include/module.modulemap. SwiftPM C targets require at least one source file.
